version: 2.1

description: >
  This orb provides a set of tools for crafting re-usable workflows.

  Such as a command `pass_fast` to assist only rerunning failed nodes of parallel jobs on job retries!

display:
  source_url: "https://github.com/ankorstore/orb-toolbelt"

orbs:
  github-cli: circleci/github-cli@2.2.0
